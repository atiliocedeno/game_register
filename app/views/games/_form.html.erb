<%= form_with(model: game, local: true) do |form| %>
  <% if game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :game_type %>
    <%= form.text_field :game_type %>
  </div>

  <div class="field">
    <%= form.label :rule %>
    <%= form.text_area :rule %>
  </div>

  <div class="field">
    <%= form.label :box_picture %>
    <%= form.file_field :box_picture %>
  </div>

  <div class="field">
    <%= form.label :rules_pdf %>
    <%= form.file_field :rules_pdf, multiple: true %>
  </div>

  <div class="field">
    <%= form.label :components_and_parts %>
    <%= form.file_field :components_and_parts, multiple:true %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
